# التحديات التي واجهتها في المشروع

## التحدي الأول: عدم توافق بيانات Roboflow مع YOLOv8

### المشكلة
عند بدء تدريب نموذج YOLOv8 على مجموعة بيانات أضرار الحاويات، ظهر التحذير التالي:

### السبب
- مجموعة البيانات تحتوي على نوعين من التعليقات (Bounding Boxes و Segmentation Masks)
- نموذج YOLOv8 Detection يتعامل فقط مع Bounding Boxes
- هذا أدى إلى نتائج غير طبيعية (mAP50 = 0.03 فقط بعد 11 Epoch)

### النتائج الأولية قبل المعالجة
| Epoch | box_loss | cls_loss | mAP50 |
|-------|----------|----------|-------|
| 1 | 1.869 | 3.433 | 0.0199 |
| 5 | 1.732 | 2.833 | 0.0170 |
| 10 | 1.560 | 2.318 | 0.0296 |

### الحل المطبق
1. إعادة تحميل مجموعة البيانات بصيغة `yolov8` فقط
2. إضافة المعامل `single_cls=True` في التدريب
3. التحقق من أن جميع التعليقات من نوع Bounding Boxes فقط

### النتائج بعد المعالجة (متوقعة)
| المقياس | قبل المعالجة | بعد المعالجة |
|---------|--------------|---------------|
| mAP50 | 0.03 | ~0.85-0.92 |
| Precision | 0.12 | ~0.88-0.94 |
| Recall | 0.07 | ~0.80-0.86 |

### الدروس المستفادة
- أهمية التحقق من توافق البيانات مع النموذج قبل التدريب
- ضرورة فهم الفرق بين أنواع التعليقات المختلفة (Detection vs Segmentation)
